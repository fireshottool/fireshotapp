        HotkeyService hotkeyService = new HotkeyService();


        Gson gson = new GsonBuilder().setPrettyPrinting().create();

        FileInputStream inputStream = new FileInputStream("C:\\Users\\niki\\fireshot\\test.json");
        InputStreamReader reader = new InputStreamReader(inputStream);

        List<Hotkey> list = new ArrayList<>();

        JsonArray array = gson.fromJson(reader, JsonArray.class);
        array.forEach(var -> {
            list.add(gson.fromJson(var, Hotkey.class));
        });
        hotkeyService.getHotkeys().addAll(list);